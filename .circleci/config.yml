version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable-20.04
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.11
          docker_layer_caching: true
      - restore_cache:
          keys:
            - v1-{{ .Branch }}
      - run:
          name: Load Docker image layer cache
          command: |
            set +o pipefail
            docker load -i /caches/app.tar | true
      - run:
          name: bauhaus
          command: |
            docker build --cache-from=app -t ghcr.io/csf-ngs/hinkskalle-dev .
      - run:
          name: Save Docker image layer cache
          command: |
            mkdir -p /tmp/caches
            docker save -o /tmp/caches/app.tar ghcr.io/csf-ngs/hinkskalle-dev
      - save_cache:
          key: v1-{{ .Branch }}-{{ epoch }}
          paths:
            - /tmp/caches/app.tar
      - when:
          condition: << pipeline.git.tag >>
          steps:
            - run:
                name: pickerldrauf
                command: |
                  docker tag ghcr.io/csf-ngs/hinkskalle-dev ghcr.io/csf-ngs/hinkskalle-dev:<< pipeline.git.tag >>
            - run:
                name: pushraus
                command:
                  echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin ghcr.io
                  docker push ghcr.io/csf-ngs/hinkskalle-dev:<< pipeline.git.tag >>
                  docker push ghcr.io/csf-ngs/hinkskalle-dev